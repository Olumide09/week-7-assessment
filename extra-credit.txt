The Doubleinsert Function would be slower because as the size of the array or elements increases the .unshift method would have more and more elements to move causing the execution time to be slower than the DoublerAppend function that has the .push method.